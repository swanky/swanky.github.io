_site
# 審查／驗收時複製出來的建置快照（_site-review、_site-public-check 等），可用 jekyll build 重建
_site-*/
.sass-cache
.jekyll-metadata
# macOS Finder 產物
.DS_Store
# Claude Code：skills 與共享設定入庫（跨電腦沿用）；僅本機權限設定不入庫
.claude/settings.local.json
.claude/image-cache/
.gstack/
.playwright-mcp/
.env
assets/facebook-archive/.env
docs/*
# 例外：CLAUDE.md 引用的精簡參考文件（其餘 docs/ 仍忽略，如企業培訓手冊）
!docs/architecture.md
!docs/design.md
!docs/gstack-skills.md

# 人類圖 bodygraph 離線渲染／驗證暫存產物（截圖、SVG、比對頁）
tools/_render/
# 行銷 context（含正職職稱，2026-07-10 遷出公開 repo；正本在 docs/ 私有 repo，此處僅供本機工具讀取）
# 例外：.agents/skills/ 是跨 agent 共用 skill 正本（見 AGENTS.md），入庫
.agents/*
!.agents/skills/
# Hermes 本機工作檔（plans 等會話產物，不入公開 repo）
.hermes/
# Hermes 寫檔時的暫存目錄（任意層級，如 _posts/.hermes-tmp.xxxxxx/）
.hermes-tmp.*/
# agent 驗證截圖：一律留在暫存區，別散落 repo 根目錄（網站圖片放 assets/ 底下）
/*.png
