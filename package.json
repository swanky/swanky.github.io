{
  "name": "swanky-github-io-tooling",
  "private": true,
  "type": "module",
  "description": "Metadata only: lets Node treat assets/js/human-design/*.js as ES modules for tests. No npm install needed.",
  "scripts": {
    "test": "node --test \"tests/human-design/*.test.mjs\""
  }
}
